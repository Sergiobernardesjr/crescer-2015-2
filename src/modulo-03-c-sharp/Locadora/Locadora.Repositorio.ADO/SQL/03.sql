CREATE TABLE Selo
(
	Id	INT	IDENTITY(1,1),
	Descricao VARCHAR(30),
	PRIMARY KEY (Id)
)

INSERT INTO Selo
	(Descricao)
VALUES
	('OURO')

INSERT INTO Selo
	(Descricao)
VALUES
	('PRATA')

INSERT INTO Selo
	(Descricao)
VALUES
	('BRONZE')

ALTER TABLE Jogo
	ADD IdSelo INT CONSTRAINT FK_IdSelo FOREIGN KEY REFERENCES Selo(Id)